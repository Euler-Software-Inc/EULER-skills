node_modules/
.DS_Store
*.log
.env
.env.local
dist/
build/
.idea/
.vscode/
*.tsbuildinfo

# Per-skill working notes (raw runs, scratch findings). Sanitized outputs
# live in <plugin>/skills/<name>/examples/ and are committed.
*/skills/*/.scratch/

# Local presentation copy of the showcase (synced from showcase/index.html, not tracked).
showcase/showcase-plugin-claude.html

# Internal planning + design docs — kept local, not published to the public repo.
docs/plans/
docs/specs/
PLAN-account-aware-skills.md

# Quality-gate run summary — written by scripts/quality-gate.mjs in CI, consumed by the PR-comment step.
qg-summary.md

# Local Claude working files (submission drafts, issue bodies, scratch) — never public.
.claude/
